# 开发变动

## 变动简要

(A simple summary about the things that this commit mainly does.)

## 指引目录

* [${changed_top_level_package_name}](#${corresponded_reference})
* ...(Repeats)

## 详细变更

### ${changed_top_level_package_name}

(Summaries the changes of each files under this package. **Simple changes/refactoring/moving should be ignored**. each file should be started with `#### ${name_of_file}`, then lays out the changes with organized list.)

## 未来突破

(Just list out the TODOs found in this review in checklist style, if completed, use `[x]`, if not, use `[ ]`.)