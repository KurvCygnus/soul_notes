apiVersion: v1
kind: Secret
metadata:
  name: soulnotes-backend-secret
type: Opaque
stringData:
  USER_NAME: "kurv"
  USER_PASSWORD: "DOOM1Sf0r3v3r"
  #! 生产必须替换为真实随机密钥 (≥32 字节), 且不应入库明文 — 这里仅作演示占位.
  HASH_KEY: "change-me-32-bytes-minimum-secret-key!!"
  ASR_CALLBACK_API_KEY: ""
